{
  "name": "qalam",
  "version": "1.0.0",
  "description": "Backend service for korrektor",
  "private": true,
  "scripts": {
    "dev": "cargo run --bin qalam-http",
    "build": "cargo build --release",
    "start": "cargo build --release && ./target/release/qalam-http",
    "lint": "cargo clippy --verbose"
  }
}
